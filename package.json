{
  "name": "ecommerece",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"wait-on -t 120000 tcp:127.0.0.1:8000 && npm run dev:frontend\""
  },
  "dependencies": {
    "axios": "^1.13.6"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
